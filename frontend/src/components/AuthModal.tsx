'use client';

import { useEffect, useState } from 'react';
import { Eye, EyeOff, X } from 'lucide-react';
import { API_URL, saveCustomerSession } from '@/lib/api';

export default function AuthModal({ close }: { close: () => void }) {
  const [mode, setMode] = useState<'login' | 'register'>('login');
  const [fullName, setFullName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState('');
  const [showPassword, setShowPassword] = useState(false); const [error, setError] = useState(''); const [loading, setLoading] = useState(false);
  useEffect(() => {
    const previousOverflow = document.body.style.overflow;
    document.body.style.overflow = 'hidden';
    const onKeyDown = (event: KeyboardEvent) => { if (event.key === 'Escape') close(); };
    window.addEventListener('keydown', onKeyDown);
    return () => {
      document.body.style.overflow = previousOverflow;
      window.removeEventListener('keydown', onKeyDown);
    };
  }, [close]);
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setLoading(true); setError(''); try { const response = await fetch(`${API_URL}/auth/${mode === 'login' ? 'login' : 'register'}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(mode === 'login' ? { email, password } : { fullName, email, password }) }); const body = await response.json(); if (!response.ok) throw new Error(body.error ?? 'Could not continue'); saveCustomerSession(body.data.accessToken, body.data.user); close(); } catch (cause) { setError(cause instanceof Error ? cause.message : 'The server is unavailable'); } finally { setLoading(false); } };
  return <div className="fixed inset-0 z-[100] grid place-items-center bg-[#1f2823]/55 p-4 backdrop-blur-sm" onMouseDown={e=>{if(e.target===e.currentTarget)close();}}><div className="grid w-full max-w-3xl overflow-hidden rounded-2xl bg-[#faf8f4] shadow-2xl md:grid-cols-[.85fr_1.15fr]"><div className="hidden bg-[#26372f] p-9 text-white md:flex md:flex-col md:justify-between"><div className="font-serif text-2xl tracking-[.2em]">AURA</div><div><div className="mb-5 h-px w-10 bg-[#d2ae72]"/><h2 className="font-serif text-3xl leading-tight">Pieces made<br/>to stay with you.</h2><p className="mt-4 text-xs leading-5 text-white/48">Sign in to save favorites, manage your bag, and follow every order.</p></div><div className="text-[9px] uppercase tracking-[.18em] text-white/30">Made with intention</div></div><form onSubmit={submit} className="relative p-7 sm:p-10"><button type="button" onClick={close} aria-label="Close" className="absolute right-5 top-5 grid size-8 place-items-center rounded-full hover:bg-black/5"><X size={18}/></button><div className="text-[10px] font-bold uppercase tracking-[.2em] text-[#927345]">Aura account</div><h2 className="mt-3 font-serif text-3xl">{mode==='login'?'Welcome back':'Join Aura'}</h2><p className="mt-2 text-xs text-black/42">{mode==='login'?'Sign in to continue your collection.':'Create an account in less than a minute.'}</p><div className="mt-7 space-y-4">{mode==='register'&&<label className="block text-xs font-semibold">Full name<input value={fullName} onChange={e=>setFullName(e.target.value)} required className="mt-2 h-11 w-full rounded-lg border border-black/10 bg-white px-3 font-normal outline-none focus:border-[#617366]" placeholder="Your full name"/></label>}<label className="block text-xs font-semibold">Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email" className="mt-2 h-11 w-full rounded-lg border border-black/10 bg-white px-3 font-normal outline-none focus:border-[#617366]" placeholder="you@example.com"/></label><label className="block text-xs font-semibold">Password<div className="relative mt-2"><input type={showPassword?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} required minLength={8} autoComplete={mode==='login'?'current-password':'new-password'} className="h-11 w-full rounded-lg border border-black/10 bg-white px-3 pr-10 font-normal outline-none focus:border-[#617366]" placeholder="At least 8 characters"/><button type="button" onClick={()=>setShowPassword(v=>!v)} className="absolute right-3 top-1/2 -translate-y-1/2 text-black/35">{showPassword?<EyeOff size={16}/>:<Eye size={16}/>}</button></div></label></div>{error&&<div className="mt-4 rounded-lg bg-rose-50 px-3 py-2.5 text-xs text-rose-700">{error}</div>}<button disabled={loading} className="mt-6 h-11 w-full rounded-full bg-[#26372f] text-xs font-semibold uppercase tracking-widest text-white disabled:opacity-50">{loading?'Please wait…':mode==='login'?'Sign in':'Create account'}</button><button type="button" onClick={()=>{setMode(v=>v==='login'?'register':'login');setError('');}} className="mt-5 w-full text-center text-xs text-black/48 hover:text-black">{mode==='login'?"New to Aura? Create an account":"Already have an account? Sign in"}</button></form></div></div>;
}
